CREATE TABLE banan_entity (
                              id INT PRIMARY KEY AUTO_INCREMENT,
                              name VARCHAR(50),
                              color VARCHAR(20),
                              age INT,
                              password VARCHAR(255)
);